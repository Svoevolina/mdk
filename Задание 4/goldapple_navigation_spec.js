describe('Navigation Test for Goldapple', () => {
    beforeEach(() => {
        // Переход на главную страницу сайта
        cy.visit('https://goldapple.ru/');
    });

    it('should navigate to the "О нас" page', () => {
        // Нажимаем на ссылку "О нас"
        cy.get('a').contains('О нас').click(); // Убедитесь, что текст ссылки правильный

        // Проверяем, что URL изменился и содержит "/about" (или другой соответствующий путь)
        cy.url().should('include', '/about'); // Замените на актуальный путь

        // Проверяем наличие заголовка или элемента на странице "О нас"
        cy.get('h1').should('contain', 'О нас'); // Замените на актуальный заголовок
    });

    it('should navigate to the "Контакты" page', () => {
        // Нажимаем на ссылку "Контакты"
        cy.get('a').contains('Контакты').click(); // Убедитесь, что текст ссылки правильный

        // Проверяем, что URL изменился и содержит "/contacts" (или другой соответствующий путь)
        cy.url().should('include', '/contacts'); // Замените на актуальный путь

        // Проверяем наличие заголовка или элемента на странице "Контакты"
        cy.get('h1').should('contain', 'Контакты'); // Замените на актуальный заголовок
    });

    it('should navigate to the "Каталог" page', () => {
        // Нажимаем на ссылку "Каталог"
        cy.get('a').contains('Каталог').click(); // Убедитесь, что текст ссылки правильный

        // Проверяем, что URL изменился и содержит "/catalog" (или другой соответствующий путь)
        cy.url().should('include', '/catalog'); // Замените на актуальный путь

        // Проверяем наличие заголовка или элемента на странице "Каталог"
        cy.get('h1').should('contain', 'Каталог'); // Замените на актуальный заголовок
    });

    it('should navigate to the "Акции" page', () => {
        // Нажимаем на ссылку "Акции"
        cy.get('a').contains('Акции').click(); // Убедитесь, что текст ссылки правильный

        // Проверяем, что URL изменился и содержит "/promotions" (или другой соответствующий путь)
        cy.url().should('include', '/promotions'); // Замените на актуальный путь

        // Проверяем наличие заголовка или элемента на странице "Акции"
        cy.get('h1').should('contain', 'Акции'); // Замените на актуальный заголовок
    });
});
